import React from 'react'

const Todo = (props) => {
        return (
                <li className="a_todo">
                        { props.todo }
                </li>
        )
}

export default Todo
